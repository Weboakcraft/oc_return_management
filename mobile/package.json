{
  "name": "return-desk-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper for the Return Desk web app",
  "scripts": {
    "web": "node scripts/sync-web.mjs",
    "init": "npm run web && npx cap add android",
    "sync": "npm run web && npx cap sync android",
    "icons": "npx @capacitor/assets generate --android --assetPath resources",
    "open": "npx cap open android",
    "apk:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "apk:release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.2.0"
  }
}
